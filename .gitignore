# Environments
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Pilot data and transcripts (not published)
pilot/
pilot.zip
pilot_runs.jsonl
pilot_runs.jsonl.*.bak
pilot_transcript.md

# Design docs (not published)
*.pdf

# v2 run data and transcripts (same "not published" policy as the pilot).
# NB: all_runs.jsonl merges the pilot rows in, so it inherits the exclusion.
arms_runs.jsonl
arms_runs.jsonl.*.bak
controls_binary_runs.jsonl
controls_ternary_c1_runs.jsonl
all_runs.jsonl
v2_runs.jsonl
arms_report.md
arms_transcript.md
controls_binary_transcript.md
v2_transcript.md

# any run backup
*.bak
